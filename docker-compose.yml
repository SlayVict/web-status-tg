version: "3.9"

services:
  bot:
    build: .
    container_name: web-status-bot
    restart: unless-stopped
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
    volumes:
      - ./data.json:/app/data.json
